# Functional Requirements

Each requirement needs a stable ID, actor, statement, rationale, priority, preconditions, expected behavior, failure and recovery behavior, dependencies, acceptance criteria, evidence source, and open questions.

Write testable outcomes. Prefer “When source TLS is incompatible, processing stops and reports source_tls_incompatible” over “Handle errors.” Avoid prescribing internal implementation unless it is an approved constraint.
