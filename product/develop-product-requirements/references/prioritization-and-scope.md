# Prioritization and Scope

Classify work as must have, should have, could have, out of scope, future consideration, or open decision unless the organization uses another explicit model.

Record the reason, decision owner, dependencies, and evidence for priority. Out-of-scope and deferred items remain visible so they are not silently reintroduced. Scope changes to an approved PRD require impact analysis.
